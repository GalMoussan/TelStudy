# Supabase Configuration
# Get these from: Project Settings → API in your Supabase dashboard

# Your Supabase project URL (public, safe to expose to browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (public, safe to expose to browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase service role key (SECRET — never expose to browser or commit to git)
# Used only in server-side code (API routes, server components)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Application URL — used for OAuth redirect callbacks
# Development: http://localhost:3000
# Production: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Note: after changing env vars in Vercel, always trigger a full redeploy
# (uncheck "use existing build cache") for changes to take effect.
