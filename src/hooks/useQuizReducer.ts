// Placeholder for useQuizReducer hook
// Implementation coming in Phase 2
import type { QuizSessionState, QuizAction } from '@/types/quiz';

export function useQuizReducer() {
  // Placeholder
}

export function quizReducer(state: QuizSessionState, action: QuizAction): QuizSessionState {
  // Placeholder
  return state;
}
